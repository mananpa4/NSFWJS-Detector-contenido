{
  "name": "@moderacion/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Worker de video: ffmpeg (frames) + batch clasificadores + agregacion + Ollama 2a opinion",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node dist/index.js",
    "test": "npm run build && node --test dist/test/*.test.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
